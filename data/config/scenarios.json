{
  "scenarios": {
    "contact-center": {
      "name": "Contact Center",
      "description": "IT helpdesk call transcripts — sentiment analysis, topic clustering, agent performance",
      "data_folder": "ContactCenter_usecase",
      "data_types": ["json"],
      "has_preprocessed": true,
      "preprocessed_files": {
        "search_index": "sample_search_index_data.json",
        "processed_data": "sample_processed_data.json",
        "key_phrases": "sample_processed_data_key_phrases.json"
      }
    },
    "mortgage-application": {
      "name": "Mortgage Application",
      "description": "Housing reports and purchase contracts — document summarization, clause extraction, risk analysis",
      "data_folder": "MortgageApplication_usecase",
      "data_types": ["pdf"],
      "has_preprocessed": false
    },
    "telecom-analysis": {
      "name": "Telecom Analysis",
      "description": "Call transcripts and audio recordings — transcription, sentiment breakdowns, topic clustering",
      "data_folder": "telecom_analysis_usecase",
      "data_types": ["json", "wav"],
      "has_preprocessed": false
    }
  },
  "data_sources": {
    "azure_search": {
      "name": "Azure AI Search",
      "description": "Connect an existing Azure AI Search index",
      "fields": ["endpoint", "table"],
      "prompts": {
        "endpoint": "Search endpoint (e.g. https://my-search.search.windows.net)",
        "table": "Index name"
      }
    },
    "fabric": {
      "name": "Microsoft Fabric",
      "description": "Connect a Fabric Lakehouse or Warehouse via SQL endpoint",
      "fields": ["endpoint", "database", "table"],
      "prompts": {
        "endpoint": "SQL endpoint (e.g. your-server.database.fabric.microsoft.com)",
        "database": "Lakehouse/Warehouse name",
        "table": "Table name"
      }
    },
    "sql": {
      "name": "SQL Database",
      "description": "Connect a SQL database via ODBC connection string",
      "fields": ["connection_string", "table"],
      "prompts": {
        "connection_string": "ODBC connection string",
        "table": "Table name"
      }
    },
    "synapse": {
      "name": "Azure Synapse Analytics",
      "description": "Connect a Synapse dedicated SQL pool",
      "fields": ["endpoint", "database", "table"],
      "prompts": {
        "endpoint": "Synapse endpoint (e.g. your-workspace.sql.azuresynapse.net)",
        "database": "Dedicated pool name",
        "table": "Table name"
      }
    }
  }
}
